# karate BARシステム — 調査・改修メモ

このファイルには、ループエンジニアリングのラウンドごとに「何を試したか／結果は
どうだったか／何を学んだか」を追記していく。同じ調査を毎回ゼロからやり直さない
ための資産。

---

## ラウンド1(2026-07-16): カメラ到着後の初期検証

### 目的
カメラ(Logitech C922 Pro Stream Webcam)を購入し実験可能になったため、
`CHECKLIST.md`/`VERIFICATION_AFTER_CAMERA.md`のうち、人間の現場作業を
必要としない項目(自動化可能な範囲)を検証する。

### やったこと
1. 環境整備: Python 3.13(`python`コマンド。`python3`はWindowsAppsの
   壊れたスタブなので使わないこと)、FFmpeg、requirements.txt一式を確認・導入
2. Mockモード回帰テスト → 問題なし
3. 実カメラでのPhase1確認(起動・クリップ生成)→ 起動はできたが…
4. 実測でフレームレートを計測 → **設定は60fpsなのに実測は約29.4fpsしか
   出ていないことを発見**
5. 原因切り分け: OpenCVのバックエンド(CAP_DSHOW vs CAP_MSMF)と
   FOURCC設定順序の組み合わせを比較実験 → `CAP_MSMF`+FOURCCを
   解像度/fpsより先に設定、で実測55〜60fpsに改善することを確認
6. `src/camera_source.py`の`RealCameraSource.start()`を修正、再検証
7. 長時間安定性テスト用の監視ハーネス(`soak_monitor.py`、health API
   ポーリング+psutilでのメモリ計測+バッファ/クリップサイズ監視)を自作し、
   30〜40分規模のテストを複数回実施
8. その過程で **Web監査サーバー(Flask開発用サーバー)が長時間稼働+
   継続アクセス時に完全にハングする** ことを発見。py-spyでスレッドダンプを
   取得し、Werkzeugのアクセスログ出力がWindows上のコンソール出力
   (colorama)の内部ロックを介して詰まっていることを特定
9. `src/web_server.py`・`central_dashboard/app.py`でWerkzeugの
   アクセスログを抑制する修正を実施、再検証(10分・30分テストで
   ハング再発なしを確認)
10. 合わせて、Webサーバーの起動失敗(ポート競合等)が発生してもこれまで
    完全にサイレントだった問題に気づいたため、`src/main.py`に起動確認
    (`_wait_for_web_server`)と警告表示・監査ログ記録を追加

### 結果(発見した問題と対応)

| 問題 | 影響 | 対応 |
|---|---|---|
| 実カメラのfps設定が実際には反映されず約29fpsしか出ない | クリップの実時間範囲が想定の約2倍になり、同期精度が崩れる。この案件の核心に関わる不具合 | `camera_source.py`: `CAP_MSMF`+FOURCC先出しに変更。修正済み・検証済み |
| Web監査サーバーが長時間稼働+アクセス継続でハング | 録画は継続するため誰も気づけないまま、iPadから監査画面に一切アクセスできなくなる | `web_server.py`/`central_dashboard/app.py`: werkzeugのアクセスログを抑制。修正済み・検証済み |
| Webサーバーの起動失敗がサイレントに握りつぶされていた | 起動時にポート競合等が起きても誰も気づけない | `main.py`: 起動後に実際にHTTP応答があるか確認し、無ければ警告+監査ログ記録。修正済み・検証済み |

### 未解決・要継続観察

- 30分テストの終盤約90秒間だけ、フレームレートが24fps程度まで一時的に
  低下する事象を1回観測。エラーは出ておらず録画・Webサーバーとも継続動作。
  USB電源管理等を疑っているが未特定。**本番前に数時間規模の連続稼働で
  再現するか確認することを推奨**(現状は30分規模の検証のみ)。

### 学んだこと・次回以降への申し送り

- このWindows環境では`python3`コマンドは使えない(WindowsAppsストアの
  スタブにリダイレクトされ動作しない)。`python`コマンドを使うこと。
- `cv2.VideoCapture.get(CAP_PROP_FPS)`は「要求した値」を返すだけで、
  実際に配信されているフレームレートを保証しない。カメラ設定を変更した
  際は、必ず実測(`read()`を一定回数呼んで所要時間を計測)で確認すること。
  ログの数値を鵜呑みにしない。
- Flaskの開発用サーバー(Werkzeug)は「開発用」と警告が出る通り、
  本番の長時間・高頻度アクセスには注意が必要。今回はアクセスログ抑制で
  回避したが、8コート運用で中央ダッシュボードが頻繁にポーリングする
  運用を考えると、将来的に本格的なWSGIサーバー(gunicorn等、ただし
  Windows対応に注意)への切り替えも検討候補。
- 長時間バックグラウンドプロセスをテストする際、`subprocess.PIPE`で
  子プロセスの標準出力を溜め込んだまま読まないと、子プロセス側の
  print()が大量に出るケースでパイプが詰まり、予期しない副作用
  (今回のハング原因の一因)を引き起こしうる。次回同様のテストをする際は
  子プロセスの標準出力を都度読み捨てるスレッドを用意するか、
  ファイルにリダイレクトすること。

### 現在の状態

- `data/clips/`・`data/buffer/`はテスト後にクリーンアップ済み
- `data/audit_log.jsonl`にはテスト中のイベントが残っている
  (本番前にクリアするか判断すること)
- 画質確認(実際の技の動きをカメラで撮影しiPadでコマ送り確認)は
  孝さん本人が実施し、「無事に動作しています」と確認済み(2026-07-16)

### 残っている人間の現場作業(未着手)

- 業者のUSBボタン到着後の同期精度実測(`VERIFICATION_AFTER_CAMERA.md`のC)
  → まず`tools/detect_keyboard_key.py`でキー名確認から
- USBケーブル抜き差しによる障害シナリオの実地確認(同D、F)
  → 現状の実装では、カメラ再接続後の自動復帰はしない設計のまま
    (`recorder.py`の`_capture_loop`はVideoCaptureを再オープンしない)
- 8コート運用時の複数台同時稼働確認(同G、複数PC・カメラが必要)

---

## ラウンド2(2026-07-18): クリップ永久保存機能の追加(応用性への第一歩)

### 目的

「良かったプレイを消えないように保存し、将来的に他の生徒・他の部活動とも
共有できる基盤にしたい」という孝さんの要望を受け、現行のFIFO(直近2回分
のみ保持)とは別に、監査担当が選んだクリップだけを永久に残せる仕組みを
追加する。将来的には空手専用ではなく他の部活動システムからも簡単に
繋げられる独立サービスに発展させたい、という大きな方向性が背景にある。

### 事前確認(AskUserQuestionで孝さんに確認した3点)

大きな設計判断が絡むため、実装前に以下を確認してから着手した:

1. **共有の置き場所・範囲**:「校内LAN限定の集約サーバー」を選択
   (現行の「クラウド送信は一切なし」という設計方針を維持する方向)
2. **今回のセッションでの実装範囲**:「最小機能のみ(保存ボタン+永久保存
   フォルダ)」を選択。校内LAN集約サーバーや複数部活動との連携は
   次ラウンド以降の課題として切り分けた
3. **未成年(生徒)映像の生徒間共有について学校側の方針**:「既に整理済み
   (保護者同意・校内規定を含め)。生徒間シェアも含めて設計してよい」との回答

→ この回答を踏まえ、今回は①の方向性を見据えつつ②の範囲だけを実装した。
③により、次ラウンド以降で生徒間シェアの設計に進んでよいという前提を得た
(ただし実際の同意取得方法・記録の所在などは孝さん側で管理されている前提。
 このシステム側では「誰の映像か」の識別・同意管理はまだ一切実装していない
 ことに注意)。

### やったこと

1. `src/config.py`に`SAVED_DIR`(`data/saved/`)・`SAVED_INDEX_PATH`
   (`data/saved/index.jsonl`)を追加
2. 新規モジュール`src/saved_clips.py`を作成。既存のFIFO管理
   (`clip_extractor.py`)には一切手を加えず、「`data/clips/`の確定クリップを
   `data/saved/`へコピーする」という完全に独立した処理にすることで、
   既存のクリップ生成・削除ロジックへの影響をゼロに抑えた
3. `src/web_server.py`に3つのAPIを追加:
   `POST /api/clips/<filename>/save`(保存)、
   `GET /api/saved`(一覧)、`GET /saved/<filename>`(配信)
4. `static/index.html`に「★ このクリップを保存」ボタンと「保存済み一覧」
   トグル表示を追加(選択中のクリップが既に保存済みの場合はボタンを隠す
   ことで、無意味な二重保存を防止)
5. `tests/test_save_clip.py`を新規作成し、
   「保存→一覧に出る→動画配信される→FIFOで元クリップが消えても
   保存済みコピーは残る」までを自動検証するテストを追加
6. 既存の`tests/test_pipeline_mock.py`・`tests/test_full_stack.py`が
   引き続き通ることを確認(既存ロジックへの影響がないことの裏付け)
7. Mockモードでサーバーを起動し、ブラウザ(Claude Browser)で実際に
   保存ボタン押下→保存済み一覧への反映→保存済みクリップの再生
   までをUI操作で確認(`read_page`/`computer`のref経由クリックが
   このセッションでは安定して動かなかったため、JavaScript直接実行
   経由でクリックイベントを発火させて検証した。動画の`readyState`が
   `HAVE_ENOUGH_DATA`になることまで確認済み)

### 実装中に見つけたバグ・その対応

| 問題 | 影響 | 対応 |
|---|---|---|
| 保存ファイル名に`COURT_NAME`(既定値"コート1"などの日本語)をそのまま使っていた | ファイル名がURLパスの一部になるため、日本語を含むと`urllib`等ASCII前提の処理が`UnicodeEncodeError`で落ちる(ブラウザの`fetch`/`<video src>`は自動エンコードするため気づきにくいが、他クライアントや将来のLAN集約サーバー連携で問題化しうる) | `saved_clips.py`: ファイル名にはコート名のSHA1ハッシュ先頭8文字(ASCII専用)を使うよう変更。人が読めるコート名は`index.jsonl`側の`court`フィールドに別途保持することで、UI表示上は従来通り日本語のコート名が見える |

### 学んだこと・次回以降への申し送り

- Claude Browser側の`computer`ツールで`read_page`から得た`ref`を使って
  クリックしようとすると、このセッションでは`screenshot`がタイムアウトし、
  `left_click`も"ref is stale"で失敗し続けた(定期ポーリング処理を
  止めても再現)。原因切り分けより検証を優先し、
  `javascript_tool`で直接`element.click()`を発火する方式に切り替えて
  検証を完了させた。次回同様の状況になったら、早めにこの代替手段に
  切り替えると時間を節約できる
- ファイル名やURLパスに使う識別子は、たとえ現状ASCIIでも「将来
  設定次第で日本語が入りうる」ものは早めにハッシュ化/サニタイズしておく
  べき、という教訓(`COURT_NAME`は`config.py`でユーザーが自由に
  書き換えられる値であり、実際に既定値が日本語だった)

### 現在の状態

- `data/`配下は今回のテスト・検証で作られたファイルをすべて削除済み
  (ラウンド1時点から残っていた`bar_clip_20260716_222700_853_0.mp4`
  ―孝さんが実機画質確認で使ったと思われるファイル―はそのまま残してある)
- `data/saved/`は空の状態(index.jsonlも生成前)

### 次ラウンド以降の候補(今回は着手していない)

- 校内LANの集約サーバー(`central_dashboard/`と同じ「別プロセス・別PC」
  パターンを踏襲する想定)へ、各コートの`data/saved/`を送る仕組み
- 空手以外の部活動システムから同じ「保存」の仕組みに接続する方法の設計
  (どこまでを「karate_bar本体」に残し、どこからを独立サービスとして
  切り出すかの線引きが必要)
- 生徒間シェアのUI・閲覧権限・生徒ごとの識別/タグ付け(今回は「保存済み
  一覧を時系列で見るだけ」の最小機能にとどめている)
- 保存クリップの容量増大への対応(現状、保存は無制限に増え続ける設計。
  ディスク容量の監視や大会後のアーカイブ運用について要検討)

---

## ラウンド3(2026-07-18): GitHubへの移行(ノートパソコンでの実行に向けて)

### 目的

「このアプリをノートパソコンで実行することを考えている。簡単に移行できる
ようにしてほしい」という要望を受け、これまでGit管理されていなかった
ローカルフォルダをGitHub経由で同期できるようにした。

### 事前確認(AskUserQuestionで確認した2点)

1. **移行方法**:「GitHubに上げて、今後はgitで同期」を選択
   (USBコピーやクラウド同期フォルダではなく、git clone/pull/pushを採用)
2. **ノートパソコンの状態**:「Python・FFmpegはすでに入っている」
   → セットアップ手順の1・2(Python/FFmpeg導入)は省略し、
     `git clone` + `pip install` のみで済む前提でREADMEを書いた

### やったこと

1. `.gitignore`を作成。`data/`(録画バッファ・確定クリップ・永久保存
   クリップ・監査ログ)を除外対象にした(映像データ・個人情報を
   GitHubに一切上げないための意図的な設計判断)
2. `git init` → 全ファイルをステージし、`git status`で`data/`が
   除外されていることを確認してからコミット(初回コミット)
3. GitHubのリポジトリ作成自体は孝さん本人に依頼した
   (`gh` CLIが環境に無いことに加え、孝さんのGitHubアカウントへの
   ログインを私が代行するのは避けるべき領域と判断したため。
   空のprivateリポジトリを作ってもらい、URLを共有してもらう形にした)
4. `https://github.com/kamuratakashi11/karate_var` をリモートに登録し、
   ブランチ名を`main`にリネームしてpush → 成功(認証は既存の
   Git Credential Manager等で自動的に通った模様、追加のログイン操作は
   発生しなかった)
5. READMEに「他のPC(ノートパソコンなど)への移行」節を追記:
   - `git clone` → `pip install -r requirements.txt` の手順
   - `data/`はgit管理外のため、過去の保存クリップ自体を持っていきたい
     場合はUSB等で別途手動コピーが必要なことを明記
   - `config.py`の機種依存設定(`BUTTON_KEY_NAME`・カメラのデバイス
     番号)はPCごとに実機再確認が必要なことを明記。`COURT_NAME`は
     既に環境変数で上書き可能な設計になっているため、複数PCで
     `config.py`本体を共有しつつコートごとに変えたい場合はこちらを
     使うと同期の衝突が起きにくい、という指針も書き添えた
6. 変更(`.gitignore`・README追記)をコミット・push済み

### 学んだこと・次回以降への申し送り

- この環境には`gh`(GitHub CLI)が入っていない。GitHub上でのリポジトリ
  作成のような「孝さんのアカウントでの操作」が必要な場面では、
  孝さん自身にブラウザで操作してもらい、結果(URLなど)だけを
  もらう形にするのが安全(ログイン代行はしない)
- `config.py`は複数PCでの共有を始めた時点で、「PCごとに変えたい設定」
  (カメラ番号・ボタンキー名など)と「揃えたい設定」が混在する
  ファイルになる。今回は`COURT_NAME`だけ環境変数化されている状態を
  活かして案内したが、複数コート運用が本格化したら、他の機種依存設定
  (`BUTTON_KEY_NAME`等)も環境変数化するかどうかは改めて検討の余地あり

### 現在の状態

- `C:\Users\k_tak\Downloads\karate_bar\karate_bar` はGitリポジトリ化済み、
  `main`ブランチが`https://github.com/kamuratakashi11/karate_var`と同期済み
- ノートパソコン側ではまだ何も作業していない(次にやること:
  上記READMEの手順で`git clone`してもらい、実際に起動確認)

---

## ラウンド4(2026-07-21): テイクモード(型の通し録画)の追加

### 目的

「型は5分程度の演武全体を録画・検証したい」という要望を受け、既存の
「F2ストップの瞬間から遡ってCLIP_DURATION_SECONDS(10秒)を切り出す」
トリガー直前保存方式(組手向け)とは別に、F2スタート〜ストップの区間を
まるごと1本のクリップとして保存する仕組みを追加した。空手専用ではなく
他の部活動でも使われうるため、名称は競技名に依存しない一般的なものにした。

- 既存方式 → **リプレイモード**(`replay`, 既定): 動作は完全に現状維持
- 新規方式 → **テイクモード**(`take`): 開始〜終了を通しで録画し、
  `data/takes/`という別枠(FIFOの対象外)に保存する

### 事前確認(AskUserQuestionで確認した4点)

1. 実装範囲: 「テイクモードのみ今回実装」(生徒間シェア・権限設計は次ラウンド)
2. テイクの保持方式: 「最初から別枠の保持エリアに入れる」
   (既存の`clips/`2枚FIFOには乗せない)
3. 保存済みクリップ(`data/saved/`)の一括削除UI: 「含める」
   (テイクにも同じ仕組みを適用)
4. モード切替の方式: 「iPad画面上の単一トグルボタン」
   (タップごとにreplay⇄takeが交互に切り替わる。物理ボタン割り当ては今回は無し)

### やったこと

1. `config.py`に`TAKE_DIR`(`data/takes/`)・`TAKE_INDEX_PATH`・
   `TAKE_MAX_DURATION_SECONDS`(10分の安全キャップ)を追加
2. `src/recording_mode.py`を新規作成。`replay`/`take`のモード状態を持つ
   薄いスレッドセーフモジュール(`web_server.py`と`main.py`の両方から
   参照するため独立させた)。録画中(`running`)はモード切替を拒否する
3. `src/recorder.py`に`pause_cleanup()`/`resume_cleanup()`を追加。
   テイク録画中はリングバッファの通常のFIFO削除(`BUFFER_SEGMENTS`超過分の
   自動削除)を一時停止し、開始〜終了の区間全体を後で切り出せるようにした
4. `src/take_recorder.py`を新規作成。`clip_extractor.py`と同じ
   「バッファのTSセグメントをconcatしてtrimする」パターンを踏襲しつつ、
   固定10秒ではなく`start_take()`〜`stop_take()`の実経過時間ぶんを
   可変長で切り出すようにした。保護期間の概念は持たず、削除は
   一括削除UIのみで行う設計
5. `src/main.py`: `on_key_event`(F2スタート検知)でテイクモード中なら
   `take_recorder.start_take()`+`recorder.pause_cleanup()`+最大時間キャップ用
   `threading.Timer`を起動。`trigger_yame`(F2ストップ/緊急ボタン)を
   モードで分岐し、テイクモードなら`take_recorder.stop_take()`を呼ぶように変更
6. `src/web_server.py`に`GET/POST /api/mode`・`GET /api/takes`・
   `GET /takes/<filename>`・`POST /api/takes/delete`・
   `POST /api/saved/delete`を追加。`/api/status`のレスポンスに
   現在のモードを含めるようにした
7. `src/saved_clips.py`に`delete_saved_clips()`を追加(一括削除UI用)
8. `static/index.html`: モード切替用の単一トグルボタンを追加。
   保存済み一覧・テイク一覧それぞれに「編集」トグル→チェックボックス選択→
   確認ダイアログ付きの一括削除ボタンを追加(共通ロジックは
   `makeManagedList()`としてまとめ、2つのリストで再利用)
9. `tests/test_take_mode.py`(新規): pause_cleanup中はバッファが
   `BUFFER_SEGMENTS`を超えて保持され続けること、可変長の経過時間ぶんが
   正しく切り出されること、resume_cleanup後にFIFO削除が再開すること、
   一覧・削除APIが正しく動くことをヘッドレスで確認
10. `tests/test_mode_api.py`(新規): `/api/mode`の切替・録画中の拒否、
    `/api/takes`系・`/api/saved/delete`のHTTP疎通をEnd-to-Endで確認
11. 既存の`tests/test_pipeline_mock.py`・`tests/test_save_clip.py`・
    `tests/test_full_stack.py`が引き続き通ることを確認(リプレイモードの
    既存ロジックには一切手を入れていないため、回帰なし)
12. Playwright(このセッションのコンテナに事前導入済みのChromium)で
    実際にページを操作し、モード切替ボタンのクリックでサーバー側の
    `/api/status`のmodeが実際に切り替わること、保存済み・テイク双方の
    一覧から実データを再生できること(video要素のsrcが正しいこと)、
    編集モードでチェック→削除すると一覧・サーバー側の両方から消え、
    再生中だった場合はプレイヤーがクリアされることを確認した
    (ブラウザ環境が無いためsrc実機・iPadでの操作感の確認は未実施)

### 学んだこと・次回以降への申し送り

- このセッションのコンテナには当初`ffmpeg`・`opencv-python`が入っておらず、
  `apt-get install ffmpeg`は`noble-updates`側の一部パッケージが404で
  失敗したが、`apt-get update`後に`--no-install-recommends`を付けたら
  通った。`pip install opencv-python-headless`はdebian管理下の`blinker`が
  衝突して失敗したため、`--ignore-installed blinker`で回避した
  (実機のWindows環境では起きない、このコンテナ固有の問題)
- Playwrightは`playwright install`を実行せず、環境に事前配置された
  `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`を
  `executable_path`で直接指定すれば動く

### 現在の状態

- `data/`配下はテスト後にクリーンアップ済み
- `data/takes/`は空の状態(index.jsonlも生成前)
- 実機(Windows PC・実カメラ・業者のUSBボタン)でのテイクモード動作確認は
  未実施(このセッションはコンテナ内でのMockカメラ検証のみ)

### 次ラウンド以降の候補(今回は着手していない)

- 生徒間シェアのUI・閲覧権限・生徒ごとの識別/タグ付け(ラウンド2から持ち越し)
- テイクモードの物理ボタン割り当て(現状はiPad画面の単一トグルボタンのみ)
- 実機でのテイクモード検証: 実際に5分程度の型を通しで録画し、
  同期精度・ディスク使用量(`pause_cleanup`中の一時的な増加)・
  `TAKE_MAX_DURATION_SECONDS`(10分)の妥当性を確認

---

## ラウンド5(2026-07-22): 新PCでの実機検証・UI改善・音声録音の追加

### 目的

孝さんが空手BARシステムを動かす実機として新しいWindows PC(学校のネット
ワーク共有ドライブ`O:`上にリポジトリを配置)を用意したため、PR #1
(ラウンド4のテイクモード)を実機で初めて検証した。検証の過程で出てきた
不具合修正・UI改善に加え、「音声も録りたい」という新規要望を受けて
音声録音機能を追加した。

### やったこと

**1. 新PCでのセットアップ**

- Gitが未インストールだったためGit for Windowsを導入
- `O:\`は`\\AZ-OKAD02\teachers$\tkamura\karate_var`へのネットワークドライブ
  だったため、`git fetch`/`checkout`が「dubious ownership」で拒否される
  → `git config --global --add safe.directory` で許可して解決
- `pip install -r requirements.txt --break-system-packages` は問題なく完了

**2. 実カメラ接続時のトラブルシュート**

- `--camera 0` では外付けカメラのランプが点かず(内蔵カメラ`HP 5MP Camera`が
  device 0として認識されていたため)。カメラ番号を機械的に特定するため、
  各indexを`cv2.VideoCapture`で開いて1枚ずつ画像保存する簡易スクリプト
  (`list_cameras.py`)をその場で作成・使用し、`--camera 1`が目的のC922
  であることを画像内容から特定した(ランプ頼みより確実)
- `--camera 1`で起動したところ2分以上「フリーズしたように」見えたが、
  各設定変更(`cap.open`・`cap.set(FOURCC/WIDTH/HEIGHT/FPS)`)ごとに
  実行時間を計測する診断スクリプト(`diag_camera.py`)を作って原因を
  切り分けたところ、**フリーズではなく、1回の設定変更に約60秒かかって
  いるだけ**と判明(合計約4分)。同じカメラ(Logicool C922)を使って
  いるにもかかわらず、以前のPC(ラウンド1)ではこの遅延は無かった
- この新PCには**AppGuard**(法人向けアプリケーション隔離セキュリティ
  ソフト)が導入されており、`python.exe`のような汎用インタプリタを
  「保護対象」として扱い、デバイスアクセスや新規プロセス起動を
  厳しく検査・遅延させていることが濃厚に疑われる
  (`set FOURCC(MJPG)`が失敗して`False`を返す症状も含め、一致)。
  無効化はせず、学校IT担当者に`python.exe`/対象フォルダのAppGuard除外を
  依頼する方向で案内した(セキュリティソフトの無効化は現場の判断で
  勝手に行うべきではないため)
- 同じAppGuardの影響と見られる症状として、`clip_extractor.py`の
  「やめ」処理(ffmpegを新規に2回起動: concat→trim)も**約25秒**かかる
  ことが判明。Enterキー入力はブロッキング処理のため、待たずに連打すると
  2回分がキューイングされて後からまとめて発火する(バグではなく、
  想定通りの2枠FIFO動作)。**孝さんによると本番用PCにはAppGuardが
  入っていないため、本番運用には影響しない見込み**

**3. UI改善(実機でのフィードバックを反映)**

- ヘッダー(モード切替・一覧セクション)が増えた影響で、縦幅の狭い
  ブラウザウィンドウでは各要素がflexboxに押しつぶされて文字が見えなく
  なる不具合を発見・修正(ヘッダー系バーに`flex-shrink: 0`を付与し、
  縮んでよいのは動画エリアだけにした)
- 「動画をもっと大きく表示したい」という要望を受け、画面を「🎬再生」
  「📋一覧・設定」の2タブに分割。クリップ選択時に自動で再生タブへ
  切り替わるようにし、動画表示領域を大幅に拡大(検証環境で437px→722px)
- 「どれがリプレイ由来でどれがテイク由来か分かりにくい」という指摘を
  受け、一覧の各ボタンに色分けタグ(リプレイ=青、テイク=オレンジ)を追加

**4. 音声録音の追加(任意設定、リプレイ・テイク両モード対応)**

- `config.py`に`AUDIO_DEVICE_NAME`(既定`None`=無効)・`AUDIO_BITRATE`を追加
- `recorder.py`のffmpegコマンド組み立てを`_build_ffmpeg_cmd()`関数に切り出し、
  `AUDIO_DEVICE_NAME`未設定時は既存と完全一致するコマンドになることを
  ユニットテスト(`tests/test_ffmpeg_cmd.py`)で担保(回帰防止)
- 設定時はdshow(Windows専用)経由でマイク入力を2つ目の入力として追加し、
  `clip_extractor.py`・`take_recorder.py`は無改修のまま(`-c copy`が
  全ストリームを引き継ぐため)音声も一緒に切り出される設計にした
- 音ズレ対策として、映像・音声の両方の入力に`-use_wallclock_as_timestamps 1`
  を付与(音声が無効なときはこれまで通り付与しない)
- Web再生画面の`<video>`から`muted`属性を削除(録っても聞こえないと
  意味がないため)
- 実機での確認手順:
  1. `ffmpeg -list_devices true -f dshow -i dummy`でマイク名を確認
     (**日本語のデバイス名が文字化けする場合、`chcp 65001`で解決**。
     このPCでは`"マイク (C922 Pro Stream Webcam)"`が正しい名前だった)
  2. `config.py`の`AUDIO_DEVICE_NAME`に設定
  3. クリップを1本作り、実際にブラウザで再生して音が聞こえるか確認
     → **実機で音声が正常に再生されることを確認済み**
  4. `ffprobe`でも`codec_type=audio`(codec_name=aac)を確認済み

### 学んだこと・次回以降への申し送り

- 「フリーズしたように見える」ときは、本当に固まっているのか単に
  遅いだけなのかを、各ステップの所要時間を計測するスクリプトで
  切り分けるのが有効(`diag_camera.py`方式)。特にこの手の法人向け
  セキュリティソフトが入ったPCでは、デバイスアクセスや新規プロセス
  起動が数十秒〜数分単位で遅延することがある
- Windows(特に日本語環境)でのffmpegデバイス列挙結果が文字化けする
  場合は、データ自体が壊れているのではなく表示側(ターミナルの
  コードページ)の問題であることが多い。`chcp 65001`で大抵解決する
- 同じ機種のカメラでも、PC側のセキュリティソフト・ドライバー次第で
  挙動(fps・MJPG対応・設定変更の速度)が大きく変わりうる。「前のPCで
  動いたから今回も同じはず」という前提を置かないこと
- ブラウザUIは、テスト用の小さいウィンドウ・スマホ横向きなど縦幅が
  制約される環境を想定して都度実機確認するのが望ましい
  (flexboxの見た目上の押しつぶれはPlaywrightのスクリーンショットでも
  検証可能)

### 現在の状態

- PR #1(テイクモード+今回の一連の修正・音声機能)は`main`未マージのまま、
  実機での動作確認まで完了
- 映像の明るさについて指摘あり(部屋の照明を明るくする方向で対応予定、
  ソフトウェア側の対応ではなく環境側の調整と判断)
- 本番用PCでの通し検証はまだ未実施(次回、AppGuardの無い本番PCで
  カメラ起動速度・クリップ切り出し速度・音声・テイクモードを
  再確認する予定)

### 新しいセッションへの引き継ぎメモ

- 作業ブランチ: `claude/handover-doc-review-ll5zvw`
- PR: https://github.com/kamuratakashi11/karate_var/pull/1
  (`main`へ未マージ。CIワークフローはリポジトリに1つも無し。レビュー
  コメント・スレッドは現時点で0件、`mergeable_state: clean`)
- PR #1の監視(`subscribe_pr_activity`・定期チェック)は、ラウンド5の
  セッションで一旦`unsubscribe`済み・定期チェックのトリガーも削除済み。
  **現時点でPR #1を監視しているセッションは無い**。新しいセッションで
  引き続き監視・自動対応してほしい場合は、そのセッションに明示的に
  「PR #1を監視して」と依頼してsubscribeし直す必要がある
- 次にやること(優先度順の目安):
  1. 本番PC(AppGuardなし)での通し検証: カメラ起動・クリップ切り出しが
     数秒以内で動くか、音声付きテイクモードを数分間録って同期ズレが
     無いか
  2. 問題が無ければPR #1を`main`にマージ
  3. 生徒間シェアのUI・閲覧権限設計(ラウンド2から持ち越し、まだ着手前)
  4. テイクモードの物理ボタン割り当て検討(現状iPad画面の単一
     トグルボタンのみ)

### 次ラウンド以降の候補

- 本番PC(AppGuard無し)での通し検証: カメラ起動・クリップ切り出しが
  想定通りの速度(数秒以内)で動くか、音声付きテイクモードで数分間の
  録画をして同期ズレが無いか
- 生徒間シェアのUI・閲覧権限・生徒ごとの識別/タグ付け(ラウンド2から持ち越し)
- テイクモードの物理ボタン割り当て(現状はiPad画面の単一トグルボタンのみ)
- 照明環境の調整(会場の明るさ次第でFFMPEG_CRFや露出設定の見直しが
  必要になる可能性)

---

# ここから karate_var_timer_sync 独自の記録

このリポジトリは`karate_var`(https://github.com/kamuratakashi11/karate_var
PR #1、コミット`c71ede8`時点)のスナップショットを起点に分岐した独立リポジトリ。
上記までの記録はすべて分岐元`karate_var`側のものなので、分岐前提の背景として
参照しつつ、以降はこのリポジトリ独自の記録として追記していく。

## ラウンド1(2026-07-22): リポジトリ分岐+自作タイマーソフトとのHTTP連携

### 目的

孝さんが自作された空手タイマーソフト(`karate-timer-system`、PyQt6製、GitHub上に
別リポジトリとして存在)と直接連携し、「タイマーのスタート/ストップを検知して
自動的に録画トリガーがかかる」挙動を試作・検証する。`karate_var`本体は他の
部活動でも使える汎用システムとしてそのまま維持したいとのことで、新規リポジトリ
として完全に分岐する方針になった。

現行の物理ボタン連動方式(`key_listener.py`)は、OSレベルのキーボードフックで
「F2キーが何回目に押されたか」を数えてスタート/ストップを推測する設計だった
(トグル式ボタンしか外部から観測できないため、押下の取りこぼしや二重検知に
弱く、緊急再同期ボタンという保険が必要だった)。今回はタイマーソフト自身の
ソースに手を入れられるため、この推測ロジックを丸ごと不要にできた。

### やったこと

1. `karate_var`のPR #1ブランチ内容(テイクモード・音声録音・破損対策・iPad
   横向き対応まで含む、検証済みの最新状態)を、このリポジトリの初期コミットとして
   スナップショット(`git archive` → 新規`git init`。karate_var側のコミット履歴は
   引き継がず、独立した新しい歴史として開始)
2. `src/timer_bridge.py`を新規作成。`key_listener.TimerSyncedKeyListener`と
   同じ役割(録画トリガー呼び出し・保護判定用の累積動作時間の追跡)を持つが、
   startとstopが明示的に区別されて届くため、トグル推測ロジックは一切持たない
   薄い実装にした。HTTPの性質上、通知が重複する可能性を考慮し、同じ状態への
   重複通知は無視する(冪等)設計にした
3. `src/web_server.py`に`POST /api/timer/event`を追加(`{"event": "start"}`/
   `{"event": "stop"}`を受け取り、`timer_bridge`へ橋渡しする)
4. `src/main.py`に`--input-mode timer`を追加。既存の`enter`/`button`モードは
   そのまま残した(動作確認・フォールバック用途で引き続き使えるようにするため)
5. **karate-timer-systemリポジトリ側にも直接変更を加えた**(`karate_scoreboard.py`):
   - `toggle_timer()`(スタート/ストップ両方を扱う唯一の関数、Space
     キー/STARTボタンから呼ばれる)の両分岐に`notify_var_system()`呼び出しを追加
   - `tick()`内の「時間切れによる自動停止」分岐にも同様に追加(手動リセット
     `reset_match()`は対象外とし、純粋なスタート/ストップのみに絞った)
   - 通知は`urllib.request`(標準ライブラリ、`requests`のような追加依存を増やさない
     ため。PyInstallerでexe化する運用のため依存追加を避けたかった)を使い、
     別スレッド+短いタイムアウト+例外握りつぶしで実装。VARシステム側が
     未起動でもタイマー本体の動作には一切影響しない設計にした
   - 「形(kata)」モードにはタイマーが存在しないため、`kata_scoreboard.py`側は
     変更していない
6. `tests/test_timer_bridge.py`を新規作成。MockカメラでStart→StopのHTTP POSTから
   クリップ/テイクが実際に生成されること、重複通知が冪等に無視されること、
   不正なevent値が400を返すことを自動検証。パス
7. 既存の回帰テスト全8本(`test_pipeline_mock.py`・`test_save_clip.py`・
   `test_ffmpeg_cmd.py`・`test_mode_api.py`・`test_take_mode.py`・
   `test_full_stack.py`・`test_clip_corruption_fallback.py`・
   `test_key_listener_nonblocking.py`)を再実行し、全てパスすることを確認
   (分岐直後のため、karate_var側からの変更点はtimer_bridge関連の追加のみ)

### 現在の状態

- Mockモードでの自動テストは完了。**実機(実際のタイマーソフト+実カメラ)での
  エンドツーエンド確認はまだ未実施**
- karate-timer-systemリポジトリ側の変更は、コミット・push前の状態
  (孝さんの確認待ち)

### 次ラウンド以降の候補

- 実際に両方のソフトを同時に起動し、タイマーのスタート/ストップ操作で
  VARシステム側のクリップが自動生成されるかを実機で確認する
- 試合番号の自動連携(`karate-timer-system`側は`set_match_data`等で試合番号を
  保持しているため、`/api/timer/event`のペイロードに`match`フィールドを
  含めて連携する余地がある。今回はスコープ外として見送った)
- VARシステムとタイマーソフトが別PCで動く構成にしたい場合の、ポート開放・
  IPアドレス指定の検討(今回は同一PC前提)
