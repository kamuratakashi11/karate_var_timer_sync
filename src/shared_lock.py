"""
バッファディレクトリ(data/buffer/)へのアクセスを排他制御するためのロック。

recorder.py の「古いセグメントを削除する処理」と、
clip_extractor.py の「セグメントを読み込んでクリップを切り出す処理」が
同時に走ると、切り出し処理が「読もうとしたファイルが削除された直後だった」
という形で失敗する可能性がある(レースコンディション)。

この2つの処理が同じタイミングで動かないよう、共通のロックを1つ用意し、
両方がこのロックを使う。
"""

import threading

buffer_lock = threading.Lock()
